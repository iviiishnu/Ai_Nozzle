// Copy this file to secrets.h (same folder) and fill in your values.
// secrets.h is git-ignored — never commit real credentials.
#pragma once

// From Blynk Console → Templates → your template → "Firmware configuration"
#define BLYNK_TEMPLATE_ID   "TMPLxxxxxxxxx"
#define BLYNK_TEMPLATE_NAME "ESP32 Motor Control"

// From Blynk Console → Devices → your device → Device info
#define BLYNK_AUTH_TOKEN    "your-device-auth-token"

// 2.4 GHz WiFi the ESP32 should join
#define WIFI_SSID "your-wifi-name"
#define WIFI_PASS "your-wifi-password"
